# Core video pipeline (Day 1)
opencv-python>=4.8.0
pyyaml>=6.0.1
numpy>=1.24.0

# Object detection (Day 2)
# NOTE for CPU-only systems: install PyTorch separately FIRST to avoid
# downloading the massive CUDA build (~2.5 GB). Run:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Then run: pip install -r requirements.txt
ultralytics>=8.0.0

# Multi-object tracking — supervision for Detections API, trackers for ByteTrack (Day 2)
# Note: sv.ByteTrack was deprecated in supervision 0.28; we use ByteTrackTracker from
# the dedicated `trackers` package (the new official home from Roboflow).
supervision>=0.21.0
trackers>=2.5.0

# Monocular depth estimation (Day 4)
# MiDaS loads via torch.hub (no extra pip package for the model itself), but its
# DPT backbone requires the `timm` library (PyTorch Image Models) at runtime.
# torch is already installed from Day 2.
timm>=0.9.0

# FastAPI service layer (Day 7)
# fastapi      — the web framework; includes Starlette for WebSocket support.
# uvicorn      — ASGI server to run FastAPI. [standard] pulls in uvloop (Linux)
#                and httptools for faster I/O; on Windows it falls back to asyncio.
# websockets   — low-level WebSocket library used by Starlette under the hood.
#                Explicitly pinned so the version is reproducible.
# python-multipart — enables FastAPI's Form / File upload parsing. Not used on
#                Day 7 but included now so a Day 9 "upload a video" route can
#                be added without an extra install step.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.0.0

